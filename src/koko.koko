// simple C-style function definition
// print(cmp(1,2));

int main() {

    int cmp(int x, int y) {
        if (x==y) {
            1
        } else {
            0
        }
    };

};

int cmp2(int x, int y) {
    if (x==y) {
        1
    } else {
        0
    }
};

y == 4;
// cmp2(1,3);

